powers:/powers = >>>

    @pinfo = pinfo(player())
    
    @playerRank = @pinfo[9][0]
    @playerName = player()
    
    if(@playerRank == "deadpool")
    {
        tmsg(@playerName, "/deadpool")
        tmsg(@playerName, "/jump 0-4")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")        
    }
    else if(@playerRank == "greenarrow")
    {
        tmsg(@playerName, "/greenarrow")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/arrow")
        tmsg(@playerName, "/arrow regen")
        tmsg(@playerName, "/arrow regen long")
        tmsg(@playerName, "/arrow swiftness")
        tmsg(@playerName, "/arrow swiftness long")
        tmsg(@playerName, "/arrow fireresist")
        tmsg(@playerName, "/arrow healing")
        tmsg(@playerName, "/arrow nightvision")
        tmsg(@playerName, "/arrow strength")
        tmsg(@playerName, "/arrow strength long")
        tmsg(@playerName, "/arrow leaping")
        tmsg(@playerName, "/arrow leaping long")
        tmsg(@playerName, "/arrow invisibility")
        tmsg(@playerName, "/arrow poison")
        tmsg(@playerName, "/arrow poison long")
        tmsg(@playerName, "/arrow weakness")
        tmsg(@playerName, "/arrow slowness")
        tmsg(@playerName, "/arrow harming")
        tmsg(@playerName, "/arrow waterbreathing")
        tmsg(@playerName, "/arrow luck")
    }
    else if(@playerRank == "captainamerica")
    {
        tmsg(@playerName, "/captainamerica")
        tmsg(@playerName, "/jump 0-5")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-4")
    }
    else if(@playerRank == "mystique")
    {
        tmsg(@playerName, "/mystique")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/disguise")
        tmsg(@playerName, "/disguiseviewself")
    }
    else if(@playerRank == "blackwidow")
    {
        tmsg(@playerName, "/blackwidow")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
    }
    else if(@playerRank == "speedy")
    {
        tmsg(@playerName, "/speedy")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/arrow")
        tmsg(@playerName, "/arrow regen")
        tmsg(@playerName, "/arrow regen long")
        tmsg(@playerName, "/arrow swiftness")
        tmsg(@playerName, "/arrow swiftness long")
        tmsg(@playerName, "/arrow fireresist")
        tmsg(@playerName, "/arrow healing")
        tmsg(@playerName, "/arrow nightvision")
        tmsg(@playerName, "/arrow strength")
        tmsg(@playerName, "/arrow strength long")
        tmsg(@playerName, "/arrow leaping")
        tmsg(@playerName, "/arrow leaping long")
        tmsg(@playerName, "/arrow invisibility")
        tmsg(@playerName, "/arrow poison")
        tmsg(@playerName, "/arrow poison long")
        tmsg(@playerName, "/arrow weakness")
        tmsg(@playerName, "/arrow slowness")
        tmsg(@playerName, "/arrow harming")
        tmsg(@playerName, "/arrow waterbreathing")
        tmsg(@playerName, "/arrow luck")
    }
    else if(@playerRank == "raven")
    {
        tmsg(@playerName, "/raven")
        tmsg(@playerName, "/jump 0-5")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "doctormanhattan")
    {
        tmsg(@playerName, "/doctormanhattan")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/gmc")
        tmsg(@playerName, "/gms")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/speak <playername>")
        tmsg(@playerName, "/intangible")
        tmsg(@playerName, "/tangible")
        tmsg(@playerName, "/invis")
        tmsg(@playerName, "/vis")
        tmsg(@playerName, "/disguise")
        tmsg(@playerName, "/disguiseviewself")
        tmsg(@playerName, "/tp")
        tmsg(@playerName, "/heal")
        tmsg(@playerName, "/repair")
        tmsg(@playerName, "/skull")
        tmsg(@playerName, "/time")
        tmsg(@playerName, "/spawner")
        tmsg(@playerName, "/kill")
        tmsg(@playerName, "/lightning")
        tmsg(@playerName, "/thunder")
        tmsg(@playerName, "/weather")
        tmsg(@playerName, "/back")
        tmsg(@playerName, "/top")
    }
    else if(@playerRank == "thanos")
    {
        tmsg(@playerName, "/thanos")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "galactus")
    {
        tmsg(@playerName, "/galactus")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "blackpanther")
    {
        tmsg(@playerName, "/blackpanther")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
    }
    else if(@playerRank == "hawkeye")
    {
        tmsg(@playerName, "/hawkeye")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/arrow")
        tmsg(@playerName, "/arrow regen")
        tmsg(@playerName, "/arrow regen long")
        tmsg(@playerName, "/arrow swiftness")
        tmsg(@playerName, "/arrow swiftness long")
        tmsg(@playerName, "/arrow fireresist")
        tmsg(@playerName, "/arrow healing")
        tmsg(@playerName, "/arrow nightvision")
        tmsg(@playerName, "/arrow strength")
        tmsg(@playerName, "/arrow strength long")
        tmsg(@playerName, "/arrow leaping")
        tmsg(@playerName, "/arrow leaping long")
        tmsg(@playerName, "/arrow invisibility")
        tmsg(@playerName, "/arrow poison")
        tmsg(@playerName, "/arrow poison long")
        tmsg(@playerName, "/arrow weakness")
        tmsg(@playerName, "/arrow slowness")
        tmsg(@playerName, "/arrow harming")
        tmsg(@playerName, "/arrow waterbreathing")
        tmsg(@playerName, "/arrow luck")
    }
    else if(@playerRank == "robin")
    {
        tmsg(@playerName, "/robin")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-1")
    }
    else if(@playerRank == "aphrodite")
    {
        tmsg(@playerName, "/aphrodite")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/heal")
        tmsg(@playerName, "/goldcarrot")
        tmsg(@playerName, "/seeds")
        tmsg(@playerName, "/wheat")
        tmsg(@playerName, "/carrot")
        tmsg(@playerName, "/meat")
        tmsg(@playerName, "/fish")
    }
     else if(@playerRank == "athena")
    {
        tmsg(@playerName, "/athena")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/feed")
    }
    else if(@playerRank == "demeter")
    {
        tmsg(@playerName, "/demeter")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/wheat")
        tmsg(@playerName, "/carrot")
        tmsg(@playerName, "/potato")
        tmsg(@playerName, "/bonemeal")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
     else if(@playerRank == "dionysus")
    {
        tmsg(@playerName, "/dionysus")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/bread")
        tmsg(@playerName, "/wine")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
     else if(@playerRank == "hera")
    {
        tmsg(@playerName, "/hera")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/pig")
        tmsg(@playerName, "/cow")
        tmsg(@playerName, "/chicken")
    }
    else if(@playerRank == "hermes")
    {
        tmsg(@playerName, "/hermes")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "hestia")
    {
        tmsg(@playerName, "/hestia")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/pork")
        tmsg(@playerName, "/eggs")
        tmsg(@playerName, "/sugar")
        tmsg(@playerName, "/milk")
        tmsg(@playerName, "/wheat")
        tmsg(@playerName, "/cocoabeans")
    }
    else if(@playerRank == "blackcanary")
    {
        tmsg(@playerName, "/blackcanary")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/push")
    }
    else if(@playerRank == "hercules")
    {
        tmsg(@playerName, "/hercules")
        tmsg(@playerName, "/jump 0-5")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-8")
    }
    else if(@playerRank == "wolverine")
    {
        tmsg(@playerName, "/wolverine")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-3")
    }
    else if(@playerRank == "spiderman")
    {
        tmsg(@playerName, "/spiderman")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/web")
    }
    else if(@playerRank == "thehumantorch")
    {
        tmsg(@playerName, "/thehumantorch")
        tmsg(@playerName, "Can throw fireballs by punching with an empty hand")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/flame <playername>")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "antman")
    {
        tmsg(@playerName, "/antman")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/undisguise")
    }
    else if(@playerRank == "howardtheduck")
    {
        tmsg(@playerName, "/howardtheduck")
    }
    else if(@playerRank == "darthvader")
    {
        tmsg(@playerName, "/darthvader")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/push <playername>")
        tmsg(@playerName, "/forcechoke <playername>")
        tmsg(@playerName, "When you punch a player empty handed, your victim is struck by lightning (force lightning)")
    }
    else if(@playerRank == "darthmaul")
    {
        tmsg(@playerName, "/darthmaul")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/push <playername>")
        tmsg(@playerName, "/forcechoke <playername>")
        tmsg(@playerName, "When you punch a player empty handed, your victim is struck by lightning (force lightning)")
        
    }
    else if(@playerRank == "darthsidious")
    {
        tmsg(@playerName, "/darthsidious")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/push <playername>")
        tmsg(@playerName, "/forcechoke <playername>")
        tmsg(@playerName, "When you punch a player empty handed, your victim is struck by lightning (force lightning)")
    }
    else if(@playerRank == "cyborg")
    {
        tmsg(@playerName, "/cyborg")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-6")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/tp")
    }
    else if(@playerRank == "starfire")
    {
        tmsg(@playerName, "/starfire")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/fly on")
        tmsg(@playername, "/fly off")
        tmsg(@playerName, "Shoots fireballs with bare hand")
    }
    else if(@playerRank == "apollo")
    {
        tmsg(@playerName, "/apollo")
        tmsg(@playerName, "/jump 0-6")
        tmsg(@playerName, "/run 0-7")
        tmsg(@playerName, "/haste 0-6")
        tmsg(@playerName, "/arrow")
        tmsg(@playerName, "/arrow regen")
        tmsg(@playerName, "/arrow regen long")
        tmsg(@playerName, "/arrow swiftness")
        tmsg(@playerName, "/arrow swiftness long")
        tmsg(@playerName, "/arrow fireresist")
        tmsg(@playerName, "/arrow healing")
        tmsg(@playerName, "/arrow nightvision")
        tmsg(@playerName, "/arrow strength")
        tmsg(@playerName, "/arrow strength long")
        tmsg(@playerName, "/arrow leaping")
        tmsg(@playerName, "/arrow leaping long")
        tmsg(@playerName, "/arrow invisibility")
        tmsg(@playerName, "/arrow poison")
        tmsg(@playerName, "/arrow poison long")
        tmsg(@playerName, "/arrow weakness")
        tmsg(@playerName, "/arrow slowness")
        tmsg(@playerName, "/arrow harming")
        tmsg(@playerName, "/arrow waterbreathing")
        tmsg(@playerName, "/arrow luck")
        tmsg(@playerName, "/fly on")
        tmsg(@playername, "/fly off")
    }
    else if(@playerRank == "artemis")
    {
        tmsg(@playerName, "/artemis")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/arrow")
        tmsg(@playerName, "/arrow regen")
        tmsg(@playerName, "/arrow regen long")
        tmsg(@playerName, "/arrow swiftness")
        tmsg(@playerName, "/arrow swiftness long")
        tmsg(@playerName, "/arrow fireresist")
        tmsg(@playerName, "/arrow healing")
        tmsg(@playerName, "/arrow nightvision")
        tmsg(@playerName, "/arrow strength")
        tmsg(@playerName, "/arrow strength long")
        tmsg(@playerName, "/arrow leaping")
        tmsg(@playerName, "/arrow leaping long")
        tmsg(@playerName, "/arrow invisibility")
        tmsg(@playerName, "/arrow poison")
        tmsg(@playerName, "/arrow poison long")
        tmsg(@playerName, "/arrow weakness")
        tmsg(@playerName, "/arrow slowness")
        tmsg(@playerName, "/arrow harming")
        tmsg(@playerName, "/arrow waterbreathing")
        tmsg(@playerName, "/arrow luck")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/feed")
    }
    else if(@playerRank == "blackspiderman")
    {
        tmsg(@playerName, "/blackspiderman")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-3")
        tmsg(@playerName, "/web")
    }
    else if(@playerRank == "nightwing")
    {
        tmsg(@playerName, "/nightwing")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
    }
    else if(@playerRank == "superman")
    {
        tmsg(@playerName, "/superman")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "shazam")
    {
        tmsg(@playerName, "/shazam")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "blackadam")
    {
        tmsg(@playerName, "/blackadam")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "hulk")
    {
        tmsg(@playerName, "/hulk")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-1")
        tmsg(@playerName, "/haste 0-9")
    }
    else if(@playerRank == "doomsday")
    {
        tmsg(@playerName, "/doomsday")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-1")
        tmsg(@playerName, "/haste 0-9")
    }
    else if(@playerRank == "wonderwoman")
    {
        tmsg(@playerName, "/wonderwoman")
        tmsg(@playerName, "/jump 0-6")
        tmsg(@playerName, "/run 0-7")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "bartallen")
    {
        tmsg(@playerName, "/ bartallen")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "barryallen")
    {
        tmsg(@playerName, "/ barryallen")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "kidflash")
    {
        tmsg(@playerName, "/kidflash")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "batman")
    {
        tmsg(@playerName, "/batman")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-2")
    }
    else if(@playerRank == "fang")
    {
        tmsg(@playerName, "/fang")
        tmsg(@playerName, "/jump 0-5")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/invis (automatically de-cloak if you move)")
        tmsg(@playerName, "/vis")
    }
    else if(@playerRank == "ironman")
    {
        tmsg(@playerName, "/ironman")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-5")
        tmsg(@playerName, "/hulkbuster")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "ichigokurosaki")
    {
        tmsg(@playerName, "/ichigokurosaki")
        tmsg(@playerName, "/jump 0-4")
        tmsg(@playerName, "/run 0-6")
        tmsg(@playerName, "/haste 0-5")
        tmsg(@playerName, "/hover on")
        tmsg(@playerName, "/hover off")
    }
    else if(@playerRank == "supergirl")
    {
        tmsg(@playerName, "/supergirl")
        tmsg(@playerName, "/jump 0-8")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/haste 0-8")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "storm")
    {
        tmsg(@playerName, "/storm")
        tmsg(@playerName, "/jump 0-4")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/lightning")
        tmsg(@playerName, "/weather")
    }
    else if(@playerRank == "ultron")
    {
        tmsg(@playerName, "/ultron")
        tmsg(@playerName, "/jump 0-6")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "martianmanhunter")
    {
        tmsg(@playerName, "/martianmanhunter")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/thru (intangible)")
        tmsg(@playerName, "/disguise")
        tmsg(@playerName, "/speak <player> <message>")
        tmsg(@playerName, "/invis")
        tmsg(@playerName, "/vis")
        tmsg(@playerName, "/disguiseviewself")
        tmsg(@playerName, "/undisguise")
    }
    else if(@playerRank == "warmachine")
    {
        tmsg(@playerName, "/warmachine")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-5")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "hephaestus")
    {
        tmsg(@playerName, "/hephaestus")
        tmsg(@playerName, "/jump 0-2")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/burn")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/enchant")
        tmsg(@playerName, "/repair")
        tmsg(@playerName, "/workbench")
        tmsg(@playerName, "/flintsteel")
    }
    else if(@playerRank == "zoom")
    {
        tmsg(@playerName, "/zoom")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "naruto")
    {
        tmsg(@playerName, "/naruto")
        tmsg(@playerName, "/rasengan (very powerful punches for 10 seconds)")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-3")
    }
    else if(@playerRank == "aquaman")
    {
        tmsg(@playerName, "/aquaman")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-3")
    }
    else if(@playerRank == "lexluthor")
    {
        tmsg(@playerName, "/lexluthor")
    }
    else if(@playerRank == "thor")
    {
        tmsg(@playerName, "/thor")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "haljordan")
    {
        tmsg(@playerName, "/haljordan")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "alanscott")
    {
        tmsg(@playerName, "/alanscott")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "ares")
    {
        tmsg(@playerName, "/ares")
        tmsg(@playerName, "/jump 0-6")
        tmsg(@playerName, "/run 0-7")
        tmsg(@playerName, "/haste 0-6")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "hades")
    {
        tmsg(@playerName, "/hades")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/burn")
        tmsg(@playerName, "/feed")
        tmsg(@playerName, "/zombie")
        tmsg(@playerName, "/heal")
        tmsg(@playerName, "/lightning")
        tmsg(@playerName, "When you punch a player empty handed, your victim is struck by lightning (force lightning)")
        tmsg(@playerName, "Mobs do not attack you when you're hades.")
    }
    else if(@playerRank == "poseidon")
    {
        tmsg(@playerName, "/poseidon")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/feed")
        tmsg(@playerName, "/lightning")
        tmsg(@playerName, "/heal")
    }
    else if(@playerRank == "goku")
    {
        tmsg(@playerName, "/goku")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/tp")
    }
    else if(@playerRank == "vegeta")
    {
        tmsg(@playerName, "/vegeta")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "zeus")
    {
        tmsg(@playerName, "/zeus")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/lightning")
    }
    else if(@playerRank == "doctorstrange")
    {
        tmsg(@playerName, "/doctorstrange")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-1")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/hover on")
        tmsg(@playerName, "/hover off")
    }
    else if(@playerRank == "boros")
    {
        tmsg(@playerName, "/boros")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "onepunchman")
    {
        tmsg(@playerName, "/onepunchman")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
    }
    else if(@playerRank == "onslaughtreborn")
    {
        tmsg(@playerName, "/onslaughtrebornphysical (brings you to Superhero City)")
        tmsg(@playerName, "/onslaughtrebornmental (brings you to The End)")
        tmsg(@playerName, "/intangible (only works in The End)")
        tmsg(@playerName, "/tangible (only works in The End)")
        tmsg(@playerName, "/invis (only works in The End)")
        tmsg(@playerName, "/vis (only works in The End")
        tmsg(@playerName, "/tp (only works in the physical world)")
        tmsg(@playerName, "/disguise (only works in the physical world)")
        tmsg(@playerName, "/gmc")
        tmsg(@playerName, "/gms")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "magneto")
    {
        tmsg(@playerName, "/magneto")
        tmsg(@playerName, "/ma (this is his iron attack!)")
        tmsg(@playerName, "/jump 0-1")
        tmsg(@playerName, "/run 0-1")
        tmsg(@playerName, "/haste 0-1")
        tmsg(@playerName, "/iron")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "daredevil")
    {
        tmsg(@playerName, "/daredevil")
    }
    else if(@playerRank == "reverseflash")
    {
        tmsg(@playerName, "/reverseflash")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/haste 0-4")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "niteowl")
    {
        tmsg(@playerName, "/niteowl")
        tmsg(@playerName, "/haste 0-1")
    }
    else if(@playerRank == "greengoblin")
    {
        tmsg(@playerName, "/greengoblin")
        tmsg(@playerName, "/fly on (simulates his hoverboard, he can't really fly)")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "wallywest")
    {
        tmsg(@playerName, "/wallywest")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/tp")
        tmsg(@playerName, "/thru (phases through walls)")
    }
    else if(@playerRank == "generalzod")
    {
        tmsg(@playerName, "/generalzod")
        tmsg(@playerName, "/run 0-9")
        tmsg(@playerName, "/jump 0-9")
        tmsg(@playerName, "/haste 0-9")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
    }
    else if(@playerRank == "sonicthehedgehog")
    {
        tmsg(@playerName, "/sonicthehedgehog")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/jump 0-6")
    }
    else if(@playerRank == "parallaxentity")
    {
        tmsg(@playerName, "/parallaxentity")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/disguise")
        tmsg(@playerName, "/disguiseviewself")
    }
    else if(@playerRank == "kentnelson")
    {
        tmsg(@playerName, "/kentnelson")
        tmsg(@playerName, "/jump 0-5")
        tmsg(@playerName, "/run 0-7")
        tmsg(@playerName, "/haste 0-7")
        tmsg(@playerName, "/gmc")
        tmsg(@playerName, "/gms")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/speak <playername>")
        tmsg(@playerName, "/intangible")
        tmsg(@playerName, "/tangible")
        tmsg(@playerName, "/invis")
        tmsg(@playerName, "/vis")
        tmsg(@playerName, "/disguise")
        tmsg(@playerName, "/undisguise")
        tmsg(@playerName, "/disguiseviewself")
        tmsg(@playerName, "/tp")
        tmsg(@playerName, "/heal")
        tmsg(@playerName, "/repair")
        tmsg(@playerName, "/skull")
        tmsg(@playerName, "/time")
        tmsg(@playerName, "/spawner")
        tmsg(@playerName, "/lightning")
        tmsg(@playerName, "/weather")
        tmsg(@playerName, "/back")
        tmsg(@playerName, "/top")
    }
	else if(@playerRank == "mrmxyzptlk") 
	{
		tmsg(@playerName, "/mrmxyzptlk")
        tmsg(@playerName, "/gmc")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/tp")
        tmsg(@playerName, "/magicmrmxyzptlksmortal <playerName>")
	}
	else if(@playerRank == "blackbolt") 
	{
		tmsg(@playerName, "/blackbolt")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/jump 0-8")
        tmsg(@playerName, "/haste 0-8")
        tmsg(@playerName, "/yell (unfinished, finished soon)")
	}
	else if(@playerRank == "deathstroke") 
	{
		tmsg(@playerName, "/deathstroke")
        tmsg(@playerName, "/run 0-3")
        tmsg(@playerName, "/jump 0-4")
        tmsg(@playerName, "/haste 0-2")
	}
    else if(@playerRank == "firefly")
    {
        tmsg(@playerName, "/firefly")
        tmsg(@playerName, "/jump 0-3")
        tmsg(@playerName, "/run 0-2")
        tmsg(@playerName, "/haste 0-2")
    }
    else if(@playerRank == "venom")
    {
        tmsg(@playerName, "/venom")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-6")
        tmsg(@playerName, "/haste 0-5")
        tmsg(@playerName, "/web")
    }
    else if(@playerRank == "atom")
    {
        tmsg(@playerName, "/atom")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/jump 0-7")
        tmsg(@playerName, "/run 0-4")
        tmsg(@playerName, "/haste 0-2")
        tmsg(@playerName, "/undisguise")
    }
    else if(@playerRank == "blacklightning")
    {
        tmsg(@playerName, "/blacklightning")
        tmsg(@playerName, "/jump 0-4")
        tmsg(@playerName, "/run 0-6")
        tmsg(@playerName, "/haste 0-3")
    }
    else if(@playerRank == "caroldanvers")
    {
        tmsg(@playerName, "/caroldanvers")
        tmsg(@playerName, "/fly on")
        tmsg(@playerName, "/fly off")
        tmsg(@playerName, "/jump 0-8")
        tmsg(@playerName, "/run 0-8")
        tmsg(@playerName, "/haste 0-8")
    }
    else if(@playerRank == "batmanbatsuit")
    {
        tmsg(@playerName, "/batmanbatsuit")
        tmsg(@playerName, "/haste 0-5")
    }
<<<
