proc(_isPlayerMod, @player,

    if(
        @player == 'Tektronic' || 
        @player == 'Hero_Of_Helix' || 
        @player == 'AceTrainerBailey' || 
     ){
        return("true")
    }
    
)
